export const reloadTris = () => {
  const buttonReload = document.createElement('button');
  buttonReload.innerHTML =
    "<img src='./public/assets/Tris/reload.png' alt'reload' />";
  return buttonReload;
};

export const reloadCatching = () => {
  const buttonReload = document.createElement('button');
  buttonReload.innerHTML =
    "<img src='./public/assets/CatchingBanana/reload.png' alt'reload' />";
  return buttonReload;
};
